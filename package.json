{
  "name": "snippetfy",
  "private": true,
  "version": "2.0.0",
  "description": "Snippetfy — nova stack (SPA + API)",
  "scripts": {
    "dev": "dotenv -e .env -- pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "db:generate": "dotenv -e .env -- pnpm --filter @snippetfy/api exec prisma generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @snippetfy/api exec prisma migrate deploy",
    "db:seed": "dotenv -e .env -- pnpm --filter @snippetfy/api exec prisma db seed",
    "db:migrate:dev": "dotenv -e .env -- pnpm --filter @snippetfy/api exec prisma migrate dev",
    "db:studio": "dotenv -e .env -- pnpm --filter @snippetfy/api exec prisma studio",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
