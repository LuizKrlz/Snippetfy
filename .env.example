# Copie para .env na raiz (usado pelo docker compose)
POSTGRES_USER=snippetfy
POSTGRES_PASSWORD=snippetfy
POSTGRES_DB=snippetfy

DATABASE_URL=postgresql://snippetfy:snippetfy@localhost:5433/snippetfy

API_PORT=4000
VITE_API_URL=http://localhost:4000

JWT_SECRET=change-me-in-production-use-long-random-string
